{
  "name": "capcalc",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "packages/calc"
  ],
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}
