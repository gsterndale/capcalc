{
  "name": "@capcalc/monorepo",
  "version": "1.0.0",
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "export": "npm run export --workspaces --if-present"
  },
  "workspaces": [
    "packages/*",
    "web"
  ],
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}
